# n8n-traceable-http-node

A community node for [n8n](https://n8n.io) that performs HTTP requests and exports trace data (method, URL, status, duration, etc.) to an external observability system.

## 🧠 Features
- Supports GET, POST, PUT, DELETE
- Sends traces to a configurable endpoint
- Optional API key authentication
- Works like the built-in HTTP Request node

## 🚀 Installation
```bash
Work In Progress
